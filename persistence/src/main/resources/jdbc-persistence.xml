<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation=" http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="saveUserSql" class="java.lang.String">
        <constructor-arg>
            <value>
                MERGE INTO Q_USER
                USING DUAL ON (ACCOUNT_ID = :accountId)
                WHEN MATCHED THEN
                    UPDATE SET NAME = :name,
                        EMAIL = :email,
                        PASSWORD = :password,
                        IS_ENABLED = :isEnabled
                WHEN NOT MATCHED THEN
                    INSERT (ACCOUNT_ID, NAME, EMAIL, PASSWORD, BIRTHDAY, IS_ENABLED)
                     VALUES (:accountId, :name, :email, :password, :birthday, :isEnabled)
            </value>
        </constructor-arg>
    </bean>

    <bean id="userSequenceNextVal" class="java.lang.String">
        <constructor-arg>
            <value>
                <![CDATA[
                     SELECT USER_SEQUENCE.nextval FROM DUAL
                ]]>
            </value>
        </constructor-arg>
    </bean>

    <bean id="findUserByEmailSql" class="java.lang.String">
        <constructor-arg>
            <value>
                <![CDATA[
                     SELECT account_id, email, password, is_enabled, name, birthday FROM Q_USER WHERE email = :email
                ]]>
            </value>
        </constructor-arg>
    </bean>

</beans>
